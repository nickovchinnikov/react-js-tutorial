import React, { ReactNode } from "react";

import { ClickCounter } from "./ClickCounter";

export default {
  title: "ClickCounter",
};

export const clickCounterWithStart = (): ReactNode => <ClickCounter />;
